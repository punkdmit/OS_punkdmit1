
}
fi
exit $1
echo $1
then
if ! [ -z $1 ] 

fi
echo "error not a number" >&2; exit 0
if ! [[ $1 =~ $re ]]; then

fi
exit 0
echo "0"
then
if [ -z $1 ]

re='^[0-9]+$'

num=$1
function exit_() {
#!/bin/bash
